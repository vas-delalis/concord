from datetime import datetime
from server.User import User

class Commendations:

    commendationsList = None

    def logCommendation(commender: User, recipient: User, datetime: datetime):
        return

    def commend(commender: User, recipient: User, datetime: datetime):
        return
    
    def commendationExists(commender: User, recipient: User):
        return