import styled from 'styled-components';

const StyledApp = styled.div`
  min-height: 100vh;
  display: flex;
`

const App = () => (
  <StyledApp>
    Hello world
  </StyledApp>
);

export default App;
